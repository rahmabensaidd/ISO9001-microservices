/*import { TestBed } from '@angular/core/testing';

import { ResumeServiceTsService } from './resume.service';

describe('ResumeServiceTsService', () => {
  let service: ResumeServiceTsService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(ResumeServiceTsService);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/
